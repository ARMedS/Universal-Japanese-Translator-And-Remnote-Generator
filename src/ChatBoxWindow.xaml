<Window x:Class="UGTLive.ChatBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Interactive Text" 
        Height="300" Width="400"
        Topmost="True"
        AllowsTransparency="True"
        Background="#80000000"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Icon="/media/Icon1.ico"
        Closing="Window_Closing">
    
    <Grid>
        <Border BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Draggable Header -->
                <Border Grid.Row="0" Height="25" Background="#CC202020" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                    <Grid>
                        <TextBlock Text="Interactive Text" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="-" Width="25" Height="25" Background="#CC3232E6" Foreground="White" FontWeight="Bold" Click="ZoomOutButton_Click"/>
                            <Button Content="+" Width="25" Height="25" Background="#CC3232E6" Foreground="White" FontWeight="Bold" Click="ZoomInButton_Click"/>
                            <Button Content="X" Width="25" Height="25" Background="#CCE63232" Foreground="White" FontWeight="Bold" Click="CloseButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Content -->
                <ScrollViewer Grid.Row="1" x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                    <RichTextBox x:Name="InteractiveTextDisplay"
                                 IsReadOnly="True"
                                 Background="Transparent"
                                 Foreground="White"
                                 BorderThickness="0"
                                 FontSize="44"
                                 FontFamily="Yu Gothic UI"
                                 Padding="10">
                        <FlowDocument>
                            <Paragraph/>
                        </FlowDocument>
                    </RichTextBox>
                </ScrollViewer>

                <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="DarkGray" ResizeBehavior="PreviousAndNext"/>

                <!-- Flashcard Output -->
                <RichTextBox Grid.Row="3" x:Name="FlashcardOutputDisplay"
                             IsReadOnly="False"
                             Background="#80000000"
                             Foreground="White"
                             BorderThickness="0"
                             FontSize="18"
                             FontFamily="Yu Gothic UI"
                             Padding="10"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto">
                    <FlowDocument>
                        <Paragraph>
                            <Run Text="Flashcard output will appear here..."/>
                        </Paragraph>
                    </FlowDocument>
                </RichTextBox>
            </Grid>
        </Border>
    </Grid>
</Window>
